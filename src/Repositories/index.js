const userRepository = require('./user-repository');
const locationRepository = require('./region-repository');
const companyRepository = require('./company-repository');
const contactRepository = require('./contact-repository');

module.exports = {
    userRepository,
    locationRepository,
    companyRepository,
    contactRepository,
}